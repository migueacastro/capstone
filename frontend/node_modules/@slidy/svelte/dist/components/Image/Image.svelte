<script context="module">
	import { getContext } from "svelte";
</script>

<script>
	export let alt = "";
	export let decoding = "auto";
	export let height = void 0;
	export let lazy = false;
	export let src = "";
	export let width = void 0;
	const classNames = getContext("classNames");
</script>

<img {alt} class={classNames["img"]} {decoding} loading={lazy ? "lazy" : undefined} {src} {width} {height} {...$$restProps} />
